# Context: alembic/versions/

## Purpose & Responsibility
Contains the versioned migration scripts that define the database schema evolution. Each script contains an `upgrade()` and `downgrade()` function.

## Key Files
- `03def5e25b87_initial_migration.py`: Sets up the initial `users` table with support for standard hashing and Copyparty's custom hashing.
